<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.2.0/fonts/remixicon.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Abhishek-Portfolio</title>
    <style>
        * {
            margin: 0;
            box-sizing: border-box;
            padding: 0;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        #main {
            position: absolute;
            width: 100%;
            height: 100%;
            background-color: black;
        }

        /* to use toggle feature */
        .dark-mode {
            background-color: black;
            color: white;
        }

        /* end of the function */

        #navbar {
            position: absolute;
            width: 100%;
            height: 35px;
            background-color: transparent;
        }

        #navbar a {
            position: relative;
            color: #fff;
            font-family: Montserrat thin;
            font-weight: 900;
            text-decoration: none;
            padding: 20px;
            top: 50%;
            left: 40px;
        }

        #navbar span a {
            position: relative;
            float: right;
            top: 20%;
            color: orange;
            left: -60px;
        }

        #main h3 {
            position: absolute;
            font-family: Montserrat Thin;
            color: #fff;
        }

        #container {
            position: absolute;
            width: 60%;
            height: 450px;
            background-color: transparent;
            top: 50%;
            right: 50%;
            transform: translate(23%, -50%);
        }

        #container h3 {
            position: relative;
            font-size: 40px;
            font-family: Montserrat thin;
            font-weight: 400;
            color: #fff;
        }

        #container h1 {
            position: relative;
            font-family: Montserrat thin;
            font-size: 60px;
            color: #fff;
            font-weight: 999;
            top: 0px;
        }

        #container p {
            position: relative;
            font-family: Montserrat thin;
            font-size: 20px;
            font-weight: 700;
            color: #fff;
            top: -10px;
            letter-spacing: 1px;
        }

        #container a {
            text-decoration: none;
            font-size: 20px;
        }

        #container a {
            color: orange;
            font-family: Montserrat thin;
        }

        #container h4 {
            position: relative;
            font-family: Montserrat thin;
            font-size: 20px;
            font-weight: 700;
            color: #fff;
            top: -19px;
        }

        #container h5 {
            position: relative;
            font-family: Montserrat thin;
            font-size: 25px;
        }

        #image {
            position: fixed;
            width: 500px;
            height: 800px;
            background-color: transparent;
            top: 45%;
            right: 20%;
            transform: translate(50%, -38%);
            overflow: hidden;
        }

        #image img {
            position: absolute;
            width: 100%;
            height: 100%;
            object-fit: cover;
            top: -80px;
        }

        #chill {
            position: absolute;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: orange;
            overflow: hidden;
        }

        i {
            position: fixed;
            font-size: 35px;
            top: 50%;
            right: 50%;
            transform: translate(50%, -50%);
        }

        #icon {
            position: absolute;
            font-size: 20px;
            color: orange;
            cursor: pointer;
        }

        .toggle {
            position: absolute;
            width: 30px;
            height: 30px;
            border: orange 1.5px solid;
            right: 0;
            margin: 22px;
            border-radius: 50%;
        }

        .ri-moon-line {
            z-index: 1;
        }

        @media (max-width: 400px) {
            #image {
                position: relative;
                right: 260px;
                opacity: .5;    
            }

            #container {
                position: absolute;
                z-index: 1;
                width: 100%;
                font-size: 10px;
                background-color: transparent;
                top: 50%;
                right: 50%;
                transform: translate(50%,-48%);          
                margin: -20px ;      
            }

            
            #navbar a {
                position: relative;
                left: 20px;
                color: orange;
            }
            
            .toggle{
                display: none;
            }

            span a{
                display: none;
            }
            
        }

        
    </style>
</head>

<body>
    <div id="main">
        <div class="toggle">
            <i id="icon" class="ri-moon-line"></i>
        </div>

        <div id="container">
            <h3 id="text1">Hi, I'm Abhishek.</h3>
            <br>
            <h1 id="text1">Designer. Product Person.</h1>
            <br>
            <p id="text1">Web Developers design and update websites and online applications using various programming languages and
                design. <a href="">See More</a> .</p>
            <br>
            <h4 id="text1" >Less moody in real life. ✌️</h4>
            <br>
            <h5 id="text1"><a href="">Learn More ></a></h5>
        </div>
    </div>
    <div id="navbar">
        <a href="">Home</a><a href="">About</a><a href="">Work</a><a href="">Writing</a>
        <span><a href="">Get In Touch</a></span>
    </div>
    <div id="image">
        <img src="/images/image.png" alt="" draggable="false" srcset="">
    </div>
    <div id="chill">
        <i class="ri-arrow-left-up-line"></i>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.3/gsap.min.js"
        integrity="sha512-gmwBmiTVER57N3jYS3LinA9eb8aHrJua5..."></script>
    <script>
        // JavaScript code goes here
        gsap.set("#chill", {
            xPercent: -50,
            yPercent: -50
        });

        const ball = document.querySelector("#chill");

        const pos = {
            x: window.innerWidth / 2,
            y: window.innerHeight / 2
        };
        const mouse = { x: pos.x, y: pos.y };
        const speed = 0.2;

        const xSet = gsap.quickSetter(chill, "x", "px");
        const ySet = gsap.quickSetter(chill, "y", "px");

        window.addEventListener("mousemove", e => {
            mouse.x = e.x;
            mouse.y = e.y;
        });

        gsap.ticker.add(() => {

            // adjust speed for higher refresh monitors
            const dt = 1.0 - Math.pow(1.0 - speed, gsap.ticker.deltaRatio());

            pos.x += (mouse.x - pos.x) * dt;
            pos.y += (mouse.y - pos.y) * dt;
            xSet(pos.x);
            ySet(pos.y);
        });

        gsap.to("#container h3", {
            duration: 2,
            ease: "power3",
            y: 15
        });

        gsap.to("#container p", {
            duration: 2,
            ease: "power3",
            y: 15
        });

        gsap.to("#container h5", {
            duration: 2,
            ease: "power3",
            y: 15
        });

        gsap.to("#container h4", {
            duration: 2,
            ease: "power3",
            y: 15
        });

        gsap.to("#container h1", {
            duration: 2,
            ease: "power3",
            y: 15
        });

        gsap.to("#chill", {
            duration: 2000000,
            rotation: 360
        });

        const toggleTarget = document.getElementById("main");
        let isBlack = true;

        toggleTarget.addEventListener('click', () => {
            if (isBlack) {
                // Add your toggle functionality here
            }
        });
    </script>
</body>

</html>